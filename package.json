{
  "name": "markdown-runner",
  "displayName": "Markdown Code Block Runner",
  "description": "Enables the seamless execution of code blocks in any language within Markdown files in VS Code",
  "publisher": "renathossain",
  "icon": "Icon.png",
  "version": "3.1.0",
  "engines": {
    "vscode": "^1.65.0"
  },
  "categories": [
    "Other"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/renathossain/vscode-markdown-runner.git"
  },
  "activationEvents": [
    "onLanguage:markdown",
    "onLanguage:quarto"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "Markdown Code Block Runner",
      "properties": {
        "markdownRunner.compilerSettings": {
          "type": "object",
          "markdownDescription": "Compiler commands are optional and run before interpreter commands. Enter the temporary file extension, then the compile command. Available placeholders: `${path}`, `${dir}`, `${name}`, `${ext}`, `${exe}`. Click ↻ to restore defaults.",
          "additionalProperties": {
            "type": "string"
          },
          "default": {
            "C": ".c; gcc ${path} -o ${dir}/${name}${exe}",
            "C++, cpp, cxx": ".cpp; g++ ${path} -o ${dir}/${name}${exe}",
            "Rust, rs": ".rs; rustc ${path} -o ${dir}/${name}${exe}",
            "Java": ".java; javac ${path}",
            "TypeScript, ts": ".ts; tsc --ignoreConfig ${path}"
          }
        },
        "markdownRunner.interpreterSettings": {
          "type": "object",
          "markdownDescription": "Interpreter commands are mandatory and run after compiler commands. Enter the temporary file extension, then the interpreter command. Available placeholders: `${path}`, `${dir}`, `${name}`, `${ext}`, `${exe}`. The first tag in each language list is used for the CodeLens button. Click ↻ to restore defaults.",
          "additionalProperties": {
            "type": "string"
          },
          "default": {
            "Python, py, py3": ".py; python ${path}",
            "C": "; ${path}${exe}",
            "Java": ".class; java -cp ${dir} ${name}",
            "C++, cpp, cxx": "; ${path}${exe}",
            "Rust, rs": "; ${path}${exe}",
            "TypeScript, ts": ".js; node ${path}",
            "PHP, php3, php4, php5": ".php; php ${path}",
            "Perl, pl, perl6": ".pl; perl ${path}",
            "R, s": ".r; Rscript ${path}",
            "Dart": ".dart; dart ${path}",
            "Groovy": ".groovy; groovy ${path}",
            "Go, golang": ".go; go run ${path}",
            "Haskell, hs": ".hs; runghc ${path}",
            "Julia": ".jl; julia ${path}",
            "Lua": ".lua; lua ${path}",
            "Ruby, rb": ".rb; ruby ${path}",
            "JavaScript, js, mjs, cjs": ".js; node ${path}",
            "Bash, sh, shell, zsh": ".sh; bash ${path}",
            "Powershell, ps1, pwsh": ".ps1; powershell -File ${path}"
          }
        },
        "markdownRunner.pythonPath": {
          "type": "boolean",
          "markdownDescription": "Enable to add the Markdown file's parent directory to Python's `sys.path`, allowing you to import modules from that directory.",
          "default": true
        },
        "markdownRunner.defaultCodes": {
          "type": "object",
          "markdownDescription": "To avoid repetitive boilerplate, prepend, wrap, or append default code to parsed code blocks, and use the placeholder `${code}` to indicate where the parsed code should be inserted.\n\nIf `${code}` is not present in the template, the default code is treated as a complete program and will run as-is, ignoring the parsed code block.\n\nExamples:\n\n- `python`: `var=123\\n${code}`\n- `cpp`: `#include <bits/stdc++.h>\\nusing namespace std;\\nint main(){\\n${code}}\\n`",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        },
        "markdownRunner.activateOnQuarto": {
          "type": "boolean",
          "markdownDescription": "Enable to activate the extension on Quarto (.qmd) files.",
          "default": true
        },
        "markdownRunner.outputEncoding": {
          "type": "string",
          "default": "utf8",
          "enum": [
            "utf8",
            "utf16le",
            "latin1",
            "ascii",
            "base64",
            "hex",
            "gbk",
            "shiftjis",
            "big5",
            "windows1252"
          ],
          "markdownDescription": "Encoding used to decode `Run on Markdown` process output via [iconv-lite](https://github.com/pillarjs/iconv-lite). Pick a common value from the list, or set any other [supported encoding](https://github.com/pillarjs/iconv-lite/wiki/Supported-Encodings) directly in `settings.json`. Change to `gbk` if non-ASCII characters appear corrupted on some Windows systems."
        },
        "markdownRunner.enabledButtons": {
          "type": "object",
          "markdownDescription": "Controls which CodeLens buttons are shown above code blocks. Disable buttons you do not use to reduce clutter. Stop, Kill, and Kill All process controls are always shown when needed for safety.",
          "properties": {
            "runBlock": {
              "type": "boolean",
              "default": true,
              "description": "Show the \"Run Block\" button."
            },
            "runInTerminal": {
              "type": "boolean",
              "default": true,
              "description": "Show the \"Run in Terminal\" button for Bash and PowerShell blocks."
            },
            "runOnMarkdown": {
              "type": "boolean",
              "default": true,
              "description": "Show the \"Run on Markdown\" button."
            },
            "copy": {
              "type": "boolean",
              "default": true,
              "description": "Show the \"Copy\" button."
            },
            "clear": {
              "type": "boolean",
              "default": true,
              "description": "Show the \"Clear\" button."
            },
            "delete": {
              "type": "boolean",
              "default": true,
              "description": "Show the \"Delete\" button."
            }
          },
          "additionalProperties": false,
          "default": {
            "runBlock": true,
            "runInTerminal": true,
            "runOnMarkdown": true,
            "copy": true,
            "clear": true,
            "delete": true
          }
        }
      }
    },
    "languages": [
      {
        "id": "quarto",
        "extensions": [
          ".qmd"
        ]
      }
    ]
  },
  "scripts": {
    "build": "vsce package",
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@eslint/markdown": "^8.0.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.9.2",
    "@types/vscode": "^1.65.0",
    "@typescript-eslint/eslint-plugin": "^8.61.0",
    "@typescript-eslint/parser": "^8.61.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "eslint": "^10.4.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "globals": "^17.6.0",
    "mocha": "^11.3.0",
    "ovsx": "^1.0.0",
    "prettier": "^3.8.3",
    "ts-loader": "^9.6.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.0",
    "webpack": "^5.107.2",
    "webpack-cli": "^7.0.3"
  },
  "dependencies": {
    "@xterm/xterm": "^6.0.0",
    "iconv-lite": "^0.7.2",
    "semaphore-async-await": "^1.5.1",
    "tree-kill": "^1.2.2"
  }
}
